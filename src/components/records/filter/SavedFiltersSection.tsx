
import React from 'react';

interface SavedFiltersSectionProps {
  onSelectPreset: (preset: string) => void;
}

export const SavedFiltersSection: React.FC<SavedFiltersSectionProps> = () => {
  // No saved filters anymore
  return null;
};
